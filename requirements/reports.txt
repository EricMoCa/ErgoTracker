# requirements/reports.txt
# Task E — reports

-r base.txt
jinja2>=3.1.0             # Plantillas HTML
matplotlib>=3.8.0         # Generación de gráficas (modo headless Agg)
reportlab>=4.1.0          # Backend PDF principal (sin dependencias de sistema)
weasyprint>=60.0          # Backend PDF alternativo con CSS completo (requiere GTK3 en Windows)
pytest>=8.0.0

# El generador detecta automáticamente el backend disponible: WeasyPrint → reportlab → HTML
# En Windows sin GTK3, reportlab es el backend activo.
# WeasyPrint con GTK3: https://github.com/tschoonj/GTK-for-Windows-Runtime-Environment-Installer
