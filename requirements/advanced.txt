# advanced_pipeline: motores de estimación de postura con GPU
pydantic>=2.5.0
loguru>=0.7.0
numpy>=1.24.0
onnxruntime>=1.17.0
opencv-python>=4.8.0
requests>=2.31.0
tqdm>=4.66.0
torch>=2.1.0
pytest>=8.0.0

# Motores avanzados — instalar manualmente desde sus repos oficiales:
#
# GVHMR (recomendado, world-grounded):
#   git clone https://github.com/zju3dv/GVHMR && pip install -e GVHMR/
#
# TRAM (alternativo GPU):
#   git clone https://github.com/yufu-wang/tram && pip install -e tram/
#
# HumanMM (video multi-plano):
#   git clone https://github.com/zhangyuhong01/HumanMM-code && pip install -e HumanMM-code/
#
# WHAM (análisis de marcha, solo Linux):
#   git clone https://github.com/yohanshin/WHAM
#
# Sin ninguno de los anteriores, el sistema usa MotionBERT Lite (CPU) como fallback.
